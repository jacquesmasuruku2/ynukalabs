name: Deploy Admin Panel to Interserver

on:
  push:
    branches: [main]
    paths:
      - 'Admin Ynuka/**'
      - '.github/workflows/deploy.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '22'

      - name: Install dependencies
        run: cd "Admin Ynuka" && npm install

      - name: Build admin panel
        run: cd "Admin Ynuka" && npm run build

      - name: Deploy dist to Interserver
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "Admin Ynuka/dist/*"
          target: "/home/ynukalab/admin.ynukalabs.com/"
          strip_components: 2
          overwrite: true

      - name: Deploy PHP API files
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: |
            Admin Ynuka/php-api/api.php
            Admin Ynuka/php-api/config.php
          target: "/home/ynukalab/admin.ynukalabs.com/api/"
          strip_components: 2
          overwrite: true

      - name: Verify deployment
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            echo "✓ Admin panel deployment complete"
            ls -la /home/ynukalab/admin.ynukalabs.com/index.html

