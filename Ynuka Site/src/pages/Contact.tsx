import ContactSection from "@/components/ContactSection";

/** Même bloc contact que l’accueil — tous les liens /contact mènent ici. */
const Contact = () => (
  <div className="bg-white dark:bg-background">
    <ContactSection />
  </div>
);

export default Contact;
